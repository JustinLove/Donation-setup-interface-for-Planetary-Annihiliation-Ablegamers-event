module Config exposing (config)

config =
  { server = "http://localhost:5000/"
  --{ server = "https://ablegamers2016.herokuapp.com/"
  , wsserver = "ws://localhost:5000/"
  --, wsserver = "ws://ablegamers2016.herokuapp.com"
  }
